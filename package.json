{
  "name": "givechi",
  "private": true,
  "type": "module",
  "description": "A vetted guide to Chicago charities and where your money goes. Static site, no runtime dependencies.",
  "scripts": {
    "build": "node build.mjs",
    "serve": "python3 -m http.server 8901 --bind 127.0.0.1",
    "audit": "node tools/audit-css.mjs && node tools/contrast.mjs",
    "test": "node tests/e2e.cjs"
  },
  "devDependencies": {
    "axe-core": "^4.13.0",
    "playwright": "^1.63.0"
  }
}
